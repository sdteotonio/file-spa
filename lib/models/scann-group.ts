export enum ScannGroupFileType {
  Component = ".component.",
  Service = ".service.",
}

export enum ScannGroupFileEnds {
  ComponentClass = ".ts",
  ComponentTemplate = ".html",
  ComponentStyle = ".scss",
}
